spring:
  application:
    name: employee-service
  cloud:
    kubernetes:
      config:
        namespace: default
        name: k-name
      discovery:
        all-namespaces: false
server:
  port: 8089
management:
  endpoint:
    restart:
      enabled: true
  endpoints:
    web:
      exposure:
        include: refresh,health,info